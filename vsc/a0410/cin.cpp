#include<stdio.h>

using namespace std;

int main(){
    int x, y;
    int sum;
    cout << "tupe a number : ";
    cin >> x;
    cout << "type another number : ";
    cin >> y;
    sum = x + y;
    cout << "sum is : " << sum;

    return 0;
}